# Active plans

This directory contains approved, near-term designs that are not fully implemented.

There are currently no active plans.

Every plan must declare status, last review date, scope, dependencies, and completion conditions. A plan is not current product behavior. When work completes, migrate durable facts to the authoritative reference documents and remove the plan.
